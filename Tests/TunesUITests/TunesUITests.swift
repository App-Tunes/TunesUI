    import XCTest
    @testable import TunesUI

    final class TunesUITests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(TunesUI().text, "Hello, World!")
        }
    }
